import { ToOptionsPipe } from './to-options.pipe';

describe('ToOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new ToOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
